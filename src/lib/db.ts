/**
 * Database client placeholder.
 *
 * P-CORNEL TRADING LIMITED is a marketing / lead-gen site that routes all
 * trades through WhatsApp, so it does not require a persistent database.
 * If you later need persistence (e.g. for trade history), install Prisma
 * and replace this stub with a real client.
 */
export const db = null;
